VITE_FIREANT_ACCESS_TOKEN=
VITE_OIDC_AUTHORITY=https://accounts.fireant.vn
VITE_OIDC_CLIENT_ID=
VITE_APP_BASE_URL=http://localhost:3000
FIREANT_ACCESS_TOKEN=
SESSION_SECRET=

# OpenAI configuration (server-side only — never exposed to the browser)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
# Default model used by the AI chatbot when the user has not chosen one yet.
# If empty, the user must pick a model from the Settings page.
OPENAI_DEFAULT_MODEL=
